---
- name: Disable SWAP (1/2) - Turn off immediately
  command: swapoff -a
  # prevent errors
  when: ansible_swaptotal_mb > 0

- name: Disable SWAP (2/2) - Remove from fstab (Permanent)
  ansible.posix.mount:
    name: "{{ item }}"
    fstype: swap
    state: absent
  loop:
    - swap
    - none

- name: Load Kernel Modules (overlay, br_netfilter)
  community.general.modprobe:
    name: "{{ item }}"
    state: present
  loop:
    - overlay
    - br_netfilter

- name: Configure Sysctl for K8s Networking
  ansible.posix.sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    state: present
    sysctl_file: /etc/sysctl.d/k8s.conf
    reload: yes
  loop:
    - { key: "net.bridge.bridge-nf-call-iptables", value: "1" }
    - { key: "net.ipv4.ip_forward", value: "1" }
    - { key: "net.bridge.bridge-nf-call-ip6tables", value: "1" }

- name: Install Containerd and Transport tools
  apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
      - containerd
    state: present
    update_cache: yes
- name: Ensure /etc/containerd exists
  file:
    path: /etc/containerd
    state: directory

# 1. JUDGE: Check the file for CORRECTNESS, not just existence.
- name: Check if containerd config is valid (contains SystemdCgroup)
  ansible.builtin.command: grep "SystemdCgroup" /etc/containerd/config.toml
  register: containerd_config_check
  check_mode: no
  ignore_errors: yes
  changed_when: false

# 2. ACT: If the check failed (stub or missing), generate the full config.
- name: Generate default containerd config (if stub or missing)
  ansible.builtin.shell: containerd config default > /etc/containerd/config.toml
  when: containerd_config_check.rc != 0
  notify: Restart Containerd

# 3. CONFIGURE: Now we know the file is the full version, so we can edit it.
- name: Set SystemdCgroup = true (critical for v1.32+)
  tags: containerd
  ansible.builtin.replace:
    path: /etc/containerd/config.toml
    regexp: "SystemdCgroup = false"
    replace: "SystemdCgroup = true"
  notify: Restart Containerd

- name: Ensure Containerd is started and enabled
  service:
    name: containerd
    state: started
    enabled: yes

- name: Ensure /etc/apt/keyrings exists
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"

- name: Add Kubernetes Apt Key
  get_url:
    url: "https://pkgs.k8s.io/core:/stable:/v{{ k8s_version }}/deb/Release.key"
    dest: /etc/apt/keyrings/kubernetes-apt-keyring.asc
    mode: "0644"

- name: Remove Legacy/Manual Kubernetes Repo Definition
  file:
    path: /etc/apt/sources.list.d/kubernetes.list
    state: absent
- name: Add Kubernetes Apt Repository
  apt_repository:
    repo: "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.asc] https://pkgs.k8s.io/core:/stable:/v{{ k8s_version }}/deb/ /"
    state: present
    filename: kubernetes

- name: Install Kubelet, Kubeadm, Kubectl
  apt:
    name:
      - "kubelet={{ k8s_version }}.*"
      - "kubeadm={{ k8s_version }}.*"
      - "kubectl={{ k8s_version }}.*"
    state: present
    update_cache: yes

- name: Hold Kubernetes Packages
  dpkg_selections:
    name: "{{ item }}"
    selection: hold
  loop:
    - kubelet
    - kubeadm
    - kubectl
